"use server";

export const signIn = () => {
  try {
    //Mutation / Database / Make a fetch
  } catch (error) {
    console.log("Error :", error);
  }
};
export const signUp = ({userData}:SignUpParams) => {
  try {
    //Mutation / Database / Make a fetch
  } catch (error) {
    console.log("Error :", error);
  }
};
